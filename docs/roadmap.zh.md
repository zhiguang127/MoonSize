# 9 月 12 日至 24 日开发安排

| 时间 | 可验收结果 |
| --- | --- |
| 9/12–14 | 分析核心、CLI、真实 wasm/wasm-gc 样例、字节统计核对 |
| 9/15–17 | 比较报告、预算检查、错误诊断、恶意长度与截断测试 |
| 9/18–20 | 浏览器体验、两个真实项目案例、必要的函数名匹配改进 |
| 9/21–22 | 固定工具链 CI、多后端核心测试、独立使用反馈 |
| 9/23–24 | 发布准备、三分钟演示、申报材料与复现说明 |

前两天的决策门槛：能分析真实产物，分项字节与原文件长度完全一致，能解释至少一次真实构建变化。若这些不成立，先修正核心范围，不扩展可视化或调用图功能。

功能优先级：统计正确性 > 真实案例 > 使用体验 > 展示 > 更多格式。

## 2026-09-13：v0.2 进度

已完成符号解码与原文回退、两个固定提交的外部项目案例、CommonMark 实际优化与输出验证、四后端本地测试、Worker 与统一资源限制、JSON schema 2、报告与复现脚本。

GitHub 仓库与远端 Actions 已于 2026-09-14 完成闭环：提交 `4e10ce1` 的运行 #3 成功并上传证据。接下来优先取得独立开发者反馈、完善三分钟演示与发布说明。source map、调用图、自动优化不是本次 v0.2 的范围。

## 2026-09-18：申报后 L2 / L3 开发

本轮实现包级函数体汇总、两侧唯一原始符号匹配、增大 / 缩小 / 单侧符号 / 未匹配分类，以及双侧数量和字节覆盖率。引用分析新增直接调用、函数引用、入口 / 声明根和已知包含路径；动态目标与无法解码的区域明确保留未知。CLI、Worker、在线和离线中英报告共用 schema 3 数据。详见 [L2 / L3 接口与边界](analysis-l2-l3.zh.md)。

函数推断匹配、retained size、source map 和自动优化仍未实现。下一阶段优先采集独立项目反馈，完善 CI Job Summary 与构建条件记录，再考虑独立 gzip / Brotli 指标；引用分析不能以未发现路径推断可删除。

## 2026-09-21：v0.3 工程决策

已实现 CLI Job Summary、绑定产物 SHA-256 的构建条件记录、条件可比性提示与可选严格检查，以及独立 raw / gzip / Brotli 指标和预算。压缩使用固定参数，记录 Node 与压缩库版本；浏览器未测量时保持未知。综合策略失败仍输出 JSON / HTML / Markdown，保留 0 / 1 / 2 退出码约定。详见 [工程决策接口](engineering-ci.zh.md)。

下一阶段先取得独立项目接入反馈，验证基准产物保存、预算阈值和失败摘要是否满足真实 PR 评审需求。推断匹配、retained size 与 source map 继续延后；本轮没有将引用未知转换为可删除结论，也未发布包。

## 2026-09-23：CI 结果文件保护

CLI 新增 `--json-file`，将 JSON 结果纳入现有输出路径检查；预算失败和核心解析失败仍可留下结构化结果。示例 Actions 改用该参数，避免 shell 重定向先于 CLI 路径检查截断输入。现有 demo 产物本地策略检查通过，Node 集成测试 37 / 37 通过。

这一轮未取得独立项目接入反馈，也未推送新工作流验证远端 Actions。发布前仍需在固定 Node 24 / MoonBit 环境跑完整工作流、检查外部案例复现，并完成三分钟演示和发布材料。

## 下一阶段的交付标准

后续按 [v0.3 交付计划与最终形态](product-plan.zh.md) 推进：先完成可安装 CLI 和真实案例的 v0.3 验收，再完成真实 PR 接入模板与独立试用。该计划基于当前实现审阅，分别定义功能、分发、CI 和发布的完成标准。

## 2026-09-23：v0.3 交付实施

M1 候选 CLI tarball 已在仓库外安装验证。M2 已用固定 MoonBit 与 Node 24.19.0 重建 CommonMark/TOML，生成 v0.3 构建记录、三项预算和故意超限证据；行为对照通过。M3 提供真实 PR 工作流模板，本地使用隔离的 MoonBit 构建覆盖五种结果。远端 Actions、独立开发者试用和正式发布继续按 [发布验收清单](release-checklist.zh.md) 跟踪。

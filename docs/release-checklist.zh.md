# v0.3 发布验收

本清单区分候选包验证与实际对外发布。每项应附当前提交、环境和产物哈希；旧版远端成功不能充作本版结果。

- [x] 固定 MoonBit 与 Node 24.19.0 本地重建；四后端核心测试各 55/55、Node 集成测试 37/37。
- [x] 候选 tarball 在仓库外安装，帮助、版本、分析、比较、记录以及退出 0/1/2 通过。
- [x] CommonMark/TOML 上游重新构建与测试；CommonMark 2,887 组行为对照通过。
- [x] CommonMark 构建记录匹配；raw/gzip/Brotli 三项预算通过，故意超限失败。
- [x] PR 流程本地模拟覆盖条件匹配、超预算、条件不同、基准缺失和损坏产物。
- [ ] 当前提交的 GitHub Actions 运行成功，并上传 demo、案例和候选包验证证据。
- [ ] 至少一位未参与实现的开发者按快速开始接入一个独立项目，记录遇到的问题与修复。
- [ ] 在干净环境复验离线 HTML、中文/英文切换和实际 PR 工作流模板。
- [ ] 确定正式 npm 包名与 Mooncakes 发布坐标，核对许可证、NOTICE、版本和 schema 兼容说明。
- [ ] 发布 npm 包和 Mooncakes 核心库，记录版本与校验值。

发布说明应直接写明 core Wasm version 1 支持范围、64 MiB 输入上限、压缩指标只在 Node CLI 测量、L2 名称匹配与 L3 引用路径的含义，以及未实现的动态目标解析。发布流程完成前，候选 tarball 保留为本地试用产物。

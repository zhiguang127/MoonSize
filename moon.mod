name = "juvenile/moonsize"

version = "0.2.0"

readme = "README.md"

license = "Apache-2.0"

keywords = [ "wasm", "size", "analysis", "ci" ]

description = "Inspect WebAssembly byte sizes and compare build artifacts with a pure MoonBit analysis core."
